<Window x:Class="CybersecurityChatbot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cybersecurity Awareness Chatbot" Height="600" Width="500" Background="#2D2D30" Foreground="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Chat display -->
            <RowDefinition Height="Auto"/>
            <!-- Input -->
        </Grid.RowDefinitions>

        <!-- Header with ASCII art -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,10">
            <TextBlock Text="CYBERSECURITY AWARENESS CHATBOT" FontSize="20" FontWeight="Bold" Foreground="#00CED1" TextAlignment="Center"/>
            <TextBlock x:Name="AsciiArtTextBlock" FontFamily="Courier New" FontSize="12" Foreground="#FFFFFF" TextAlignment="Center"/>
        </StackPanel>

        <!-- Chat display area -->
        <Border Grid.Row="1" BorderBrush="#00CED1" BorderThickness="1" CornerRadius="5" Padding="5" Background="#1E1E1E">
            <ScrollViewer x:Name="ChatScrollViewer" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ChatStackPanel"/>
            </ScrollViewer>
        </Border>

        <!-- User input and send button -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBox x:Name="UserInputTextBox" Width="350" Height="30" FontSize="14" Margin="0,0,10,0" KeyDown="UserInputTextBox_KeyDown"/>
            <Button Content="Send" Width="100" Height="30" Click="SendButton_Click"/>
        </StackPanel>
    </Grid>
</Window>